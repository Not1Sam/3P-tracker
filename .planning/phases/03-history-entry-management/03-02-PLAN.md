---
phase: 03-history-entry-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(tabs)/_layout.tsx
  - app/(tabs)/calendar.tsx
  - app/(tabs)/history.tsx
  - app/entry/day/[date].tsx
  - src/screens/CalendarScreen.tsx
  - src/screens/HistoryScreen.tsx
  - src/components/history/EntryCard.tsx
  - src/components/history/SwipeableEntryCard.tsx
  - src/components/history/DateSectionHeader.tsx
autonomous: true
requirements: [LOG-10]

must_haves:
  truths:
    - "Calendar month grid shows brown dots for poop days and yellow dots for piss days"
    - "Tapping a calendar day navigates to a full-screen day detail view"
    - "Day detail view shows all entries for that date with type, time, and location"
    - "List view groups entries by date with sticky headers (Today, Yesterday, Aug 5)"
    - "Entry cards show Bristol emoji or color swatch, timestamp, type name, city, comment preview"
    - "Swiping left on an entry card reveals a delete button"
    - "Delete triggers undo toast with 3-second window"
  artifacts:
    - "src/screens/CalendarScreen.tsx"
    - "src/screens/HistoryScreen.tsx"
    - "src/components/history/EntryCard.tsx"
    - "src/components/history/SwipeableEntryCard.tsx"
    - "src/components/history/DateSectionHeader.tsx"
    - "app/entry/day/[date].tsx"
    - "app/(tabs)/calendar.tsx"
    - "app/(tabs)/history.tsx"
    - "Updated app/(tabs)/_layout.tsx"
  key_links:
    - "CalendarScreen calls getCalendarMarkedDates from history-service"
    - "HistoryScreen calls getEntriesPaginated from history-service"
    - "SwipeableEntryCard wraps EntryCard with ReanimatedSwipeable and delete+undo"
    - "Calendar onDayPress navigates to /entry/day/[date]"
    - "Day detail screen calls getEntriesForDate and renders EntryCard list"
---

<objective>
Build the calendar tab with multi-dot month grid, the list view tab with date-grouped SectionList, the day detail screen, and entry card components with swipe-to-delete.

Purpose: These are the two primary browsing surfaces for Phase 3 (LOG-10). The calendar gives at-a-glance pattern visibility; the list gives chronological browsing with rich entry context. The day detail bridges the two by showing all entries for a tapped day.

Output: 9 new files (2 screens, 3 components, 2 tab routes, 1 day detail route, 1 modified layout).
</objective>

<execution_context>
@/home/bingus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bingus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-history-entry-management/03-CONTEXT.md
@.planning/phases/03-history-entry-management/03-RESEARCH.md
@.planning/phases/03-history-entry-management/03-01-SUMMARY.md
@src/db/schema/index.ts
@src/db/repositories/poop-repository.ts
@src/db/repositories/piss-repository.ts
@src/services/history-service.ts
@src/utils/date-helpers.ts
@src/types/logging.ts
@src/constants/bristol-chart.ts
@src/constants/color-palette.ts
@src/components/common/Toast.tsx
@src/components/common/FloatingActionButton.tsx
@src/components/common/BottomSheet.tsx
@app/(tabs)/_layout.tsx
@app/(tabs)/index.tsx
@app/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry Card Components + Date Section Header</name>
  <files>src/components/history/EntryCard.tsx, src/components/history/SwipeableEntryCard.tsx, src/components/history/DateSectionHeader.tsx</files>
  <read_first>
    - src/types/logging.ts
    - src/constants/bristol-chart.ts
    - src/constants/color-palette.ts
    - src/components/common/Toast.tsx
    - src/services/history-service.ts
    - src/utils/date-helpers.ts
  </read_first>
  <action>
**Step 1 — Create `src/components/history/EntryCard.tsx`** (per D-04):

Props: `{ entry: PoopLogEntry | PissLogEntry; type: LogType; onPress: () => void }`

Renders a TouchableOpacity card with:
- Left indicator column: For poop entries, render `💩` emoji + Bristol type label (e.g. "Type 4") using `getBristolType(entry.typeId)` from `@/constants/bristol-chart`. For piss entries, render a 32x32 color swatch circle using `getPissColorHex(entry.colorId)` from `@/constants/color-palette` + color name label.
- Right content column: `formatEntryTime(entry.timestamp)` in bold, location with `📍 {entry.locationCity}` if locationCity exists, truncated comment preview (`numberOfLines={1}`) if comment exists.
- Card styling: white background, borderRadius 12, padding 12, marginBottom 8, shadow (shadowColor #000, offset {0,1}, opacity 0.08, radius 2).
- Accessibility: `accessibilityLabel` combining type, time, and city; `accessibilityRole="button"`.

**Step 2 — Create `src/components/history/SwipeableEntryCard.tsx`** (per D-08):

Props: `{ entry: PoopLogEntry | PissLogEntry; type: LogType; onPress: () => void; onDelete: (id: string, type: LogType) => void }`

Wraps `EntryCard` with `ReanimatedSwipeable` from `react-native-gesture-handler`:
- `renderRightActions`: Renders a red "Delete" button (width 80, backgroundColor #FF3B30, justifyContent center, alignItems center) that appears when swiped left.
- `rightThreshold={40}`, `friction={2}` for natural feel.
- Delete button `onPress` calls `onDelete(entry.id, type)`.
- Import `ReanimatedSwipeable` from `react-native-gesture-handler`.
- Wrap the entire component in `GestureHandlerRootView` is NOT needed here (already wrapped at app level by react-native-gesture-handler setup).

**Step 3 — Create `src/components/history/DateSectionHeader.tsx`** (per D-03):

Props: `{ title: string }`

Renders a sticky section header with:
- Text `title` (e.g. "Today", "Yesterday", "Aug 5") in bold, fontSize 16, color #333.
- Background color #F5F5F5, paddingVertical 8, paddingHorizontal 16.
- BorderBottomWidth 1, borderBottomColor #E0E0E0.
- This is used by SectionList's `renderSectionHeader`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/history/EntryCard.tsx` exists and exports `EntryCard` component
    - EntryCard renders poop emoji + Bristol type for poop entries
    - EntryCard renders color swatch circle + color name for piss entries
    - EntryCard shows formatted time, location (if present), and comment preview (if present)
    - `src/components/history/SwipeableEntryCard.tsx` exists and exports `SwipeableEntryCard` component
    - SwipeableEntryCard wraps EntryCard with ReanimatedSwipeable
    - Swipe left reveals red "Delete" button
    - `src/components/history/DateSectionHeader.tsx` exists and exports `DateSectionHeader` component
    - DateSectionHeader renders title text with sticky header styling
    - `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>
  <done>All three components compile with correct props and styling. TypeScript zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar Tab + Day Detail + History Tab + Layout</name>
  <files>app/(tabs)/_layout.tsx, app/(tabs)/calendar.tsx, app/(tabs)/history.tsx, app/entry/day/[date].tsx, src/screens/CalendarScreen.tsx, src/screens/HistoryScreen.tsx</files>
  <read_first>
    - app/(tabs)/_layout.tsx
    - src/services/history-service.ts
    - src/utils/date-helpers.ts
    - src/components/history/EntryCard.tsx
    - src/components/history/SwipeableEntryCard.tsx
    - src/components/history/DateSectionHeader.tsx
    - src/types/logging.ts
    - app/_layout.tsx
  </read_first>
  <action>
**Step 1 — Create `src/screens/CalendarScreen.tsx`** (per D-01, D-02):

- Import `Calendar` and `MarkedDates` from `react-native-calendars`.
- Import `getCalendarMarkedDates` from `@/services/history-service`.
- Import `useFocusEffect` from `expo-router`.
- State: `markedDates: MarkedDates`, `currentMonth: { year, month }`.
- `useFocusEffect` to refetch calendar data when screen gains focus (Pitfall 3 from RESEARCH.md).
- `loadCalendarData(year, month)`: Calls `getCalendarMarkedDates(year, month)`, builds `MarkedDates` object with dots: `{ key: 'poop', color: '#8B4513' }` for poop dates, `{ key: 'piss', color: '#FFD700' }` for piss dates. Per D-01: brown dot = poop, yellow dot = piss.
- `handleDayPress(day)`: Calls `router.push(`/entry/day/${day.dateString}`)`. Per D-02: full-screen day detail.
- `handleMonthChange(month)`: Updates currentMonth state.
- Render `<Calendar markingType="multi-dot" markedDates={markedDates} onDayPress={handleDayPress} onMonthChange={handleMonthChange} enableSwipeMonths={true} theme={{ dotStyle: { width: 6, height: 6, borderRadius: 3 }, todayTextColor: '#FF4500', arrowColor: '#FF4500' }} />`.
- Empty state: When no entries exist, calendar still renders normally (dots just absent).

**Step 2 — Create `src/screens/HistoryScreen.tsx`** (per D-03, D-04, D-08):

- Import `SectionList` from `react-native`.
- Import `getEntriesPaginated` from `@/services/history-service` and `groupEntriesByDate` from `@/utils/date-helpers`.
- Import `SwipeableEntryCard`, `DateSectionHeader` from `@/components/history/`.
- Import `Toast` from `@/components/common/Toast`.
- State: `sections`, `loading`, `toastVisible`, `toastMessage`, `pendingUndo`.
- `useFocusEffect` to refetch entries on focus.
- `loadEntries()`: Calls `getEntriesPaginated(50, 0)`, passes result to `groupEntriesByDate()` to produce sections.
- `handleEntryPress(entry, type)`: Navigates to `/entry/${entry.id}?type=${type}`.
- `handleDelete(id, type)`: Calls `deleteEntryWithUndo` from history-service. Shows toast with undo callback.
- Render: `<SectionList sections={sections} keyExtractor={(item) => item.id} renderItem={({ item }) => <SwipeableEntryCard entry={item} type={item._type} onPress={...} onDelete={handleDelete} />} renderSectionHeader={({ section }) => <DateSectionHeader title={section.title} />} stickySectionHeadersEnabled={true} initialNumToRender={20} maxToRenderPerBatch={10} windowSize={5} />`. Per Pitfall 2: virtualization settings.
- Toast component rendered at top level for delete undo (reuses Phase 2 Toast pattern per D-08).
- Empty state: "No entries yet. Start logging!" centered text.

**Step 3 — Create `app/entry/day/[date].tsx`** (per D-02):

- Expo Router dynamic route with `useLocalSearchParams<{ date: string }>()`.
- Import `getEntriesForDate` from `@/services/history-service`.
- Import `EntryCard` from `@/components/history/EntryCard`.
- Import `Toast` from `@/components/common/Toast`.
- Parse `date` string to Date object: `new Date(date + 'T12:00:00')` (noon to avoid timezone edge cases).
- State: `poopEntries`, `pissEntries`, `loading`, toast state.
- `useEffect` to load entries on mount.
- `handleEntryPress(entry, type)`: Navigates to `/entry/${entry.id}?type=${type}`.
- `handleDelete`: Same undo toast pattern as HistoryScreen.
- Render: Header with date title (formatted via `formatDateHeader`), ScrollView containing two sections (Poop entries with 💩 header, Piss entries with 🚽 header), each rendering `EntryCard` items. Toast for undo.
- Back navigation: Use `router.back()` from expo-router.

**Step 4 — Create `app/(tabs)/calendar.tsx`** and `app/(tabs)/history.tsx`**:

These are thin Expo Router tab screens that import and render the corresponding screen components:
- `calendar.tsx`: `import { CalendarScreen } from '@/screens/CalendarScreen'; export default CalendarScreen;`
- `history.tsx`: `import { HistoryScreen } from '@/screens/HistoryScreen'; export default HistoryScreen;`

**Step 5 — Update `app/(tabs)/_layout.tsx`** to add Calendar and History tabs:

Add two new `Tabs.Screen` entries after the existing Profile tab:
- Calendar tab: `name="calendar"`, title "Calendar", icon "calendar" from MaterialCommunityIcons, tabBarActiveTintColor '#FF4500'.
- History tab: `name="history"`, title "History", icon "format-list-bulleted" from MaterialCommunityIcons, tabBarActiveTintColor '#8B4513'.

Final tab order: Poop, Piss, Calendar, History, Profile.

**Step 6 — Update `app/_layout.tsx`** to add the entry route:

Add `<Stack.Screen name="entry" options={{ headerShown: false }} />` to the root Stack layout. This registers the `app/entry/` route group for day detail and entry detail screens.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/CalendarScreen.tsx` exists and renders react-native-calendars Calendar
    - CalendarScreen loads marked dates from history-service and shows brown/yellow dots
    - CalendarScreen navigates to `/entry/day/[date]` on day press
    - CalendarScreen refreshes data on screen focus via useFocusEffect
    - `src/screens/HistoryScreen.tsx` exists and renders SectionList
    - HistoryScreen groups entries by date with DateSectionHeader
    - HistoryScreen shows SwipeableEntryCard with swipe-to-delete
    - HistoryScreen shows Toast with undo on delete
    - HistoryScreen has virtualization settings (initialNumToRender=20)
    - `app/entry/day/[date].tsx` exists and shows entries for tapped day
    - Day detail screen navigates to individual entry detail on card press
    - `app/(tabs)/calendar.tsx` and `app/(tabs)/history.tsx` exist as tab routes
    - `app/(tabs)/_layout.tsx` has 5 tabs: Poop, Piss, Calendar, History, Profile
    - `app/_layout.tsx` includes entry route in Stack
    - `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>
  <done>Calendar shows dots, list groups entries, day detail shows entries for date, swipe-to-delete works with undo toast. All 5 tabs visible. TypeScript zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User swipe gesture → Delete action | Horizontal swipe on entry card triggers destructive delete |
| Calendar day tap → Navigation | User input triggers navigation to day detail screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | SwipeableEntryCard delete | medium | mitigate | rightThreshold=40 prevents accidental swipes; delete shows undo toast with 3s window; entry data fetched before delete for restoration |
| T-03-05 | Tampering | Calendar dot data | low | accept | Calendar dots are read-only display; no user can inject data through calendar interaction |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Calendar renders with brown/yellow dots for days with entries
- Tapping a day navigates to day detail screen showing entries
- List view shows entries grouped by date with sticky headers
- Entry cards display Bristol emoji/color swatch, time, city, comment
- Swipe left reveals red delete button
- Delete shows undo toast
</verification>

<success_criteria>
- Calendar tab works with multi-dot marking (D-01)
- Day tap navigates to full-screen detail (D-02)
- List view groups entries by date (D-03)
- Entry cards show rich info (D-04)
- Swipe-to-delete with undo works (D-08)
- All 5 tabs visible in tab bar
</success_criteria>

<output>
Create `.planning/phases/03-history-entry-management/03-02-SUMMARY.md` when done
</output>
